<div class="navbar bg-white breadcrumb-bar">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item">
        <a href="index.html"><%= current_tenant.name %></a>
      </li>
      <li class="breadcrumb-item">
        <a href="pages-app.html">User Name</a>
      </li>
      <li class="breadcrumb-item active" aria-current="page">Teams</li>
    </ol>
  </nav>

  <div class="dropdown">
    <button class="btn btn-round" role="button" data-toggle="dropdown" aria-expanded="false">
      <i class="material-icons">settings</i>
    </button>
    <div class="dropdown-menu dropdown-menu-right">
      <a class="dropdown-item" href="nav-side-team.html#" data-toggle="modal" data-target="#team-manage-modal">Edit Team</a>
      <a class="dropdown-item" href="nav-side-team.html#">Share</a>
      <div class="dropdown-divider"></div>
      <a class="dropdown-item text-danger" href="nav-side-team.html#">Leave</a>
    </div>
  </div>

</div>

<div class="container">



<%- model_class = Team -%>
<div class="page-header">
  <h1><%= @team.name %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:name) %>:</strong></dt>
  <dd><%= @team.name %></dd>
  <dt><strong><%= model_class.human_attribute_name(:description) %>:</strong></dt>
  <dd><%= @team.description %></dd>
  <dt><strong><%= model_class.human_attribute_name(:image_logo) %>:</strong></dt>
  <dd><%= @team.image_logo %></dd>
  <dt><strong><%= model_class.human_attribute_name(:image_hero) %>:</strong></dt>
  <dd><%= @team.image_hero %></dd>
  <dt><strong><%= model_class.human_attribute_name(:tenant_id) %>:</strong></dt>
  <dd><%= @team.tenant_id %></dd>
</dl>

<%= link_to "Organisation home", root_path, :class => 'btn btn-default'  %>

<%= link_to 'edit team users', users_tenant_team_path(@team.id), class: "btn btn-primary"  %>

<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_tenant_team_path(team: @team, tenant: @team.tenant_id), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              tenant_team_path(team: @team, tenant: @team.tenant_id),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>

</div>
