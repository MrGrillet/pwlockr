<%= form_with(model: user_team, local: true) do |form| %>
  <% if user_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_team.errors.count, "error") %> prohibited this user_team from being saved:</h2>

      <ul>
      <% user_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :user_team_project_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_team_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
